# One ignored pin (hookpin: ignore) alongside one stale pin that should be updated.
repos:
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.0.0
    hooks:
      - id: mypy
        additional_dependencies:
          - pydantic==1.0.0  # hookpin: ignore
          - ruff==0.8.0
